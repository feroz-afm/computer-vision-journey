pages=(list(int(input("Enter number of processes: ").split(','b)))

frames=int(inpput("Enter number of frames:"))
frames[]


for i in range n:
    if page not in frames:
        page+=page
  else 
    pages.remove(page)
    page.apned( page)
        
        if pages found in frames
        page pop(-1)
        
    else :
        page remove(page)
        pages.append(page)
        
        print(" The number of page faults ",page_fault)
        print(" The total page arround")
        
  